spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/clinica_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:clinica_user}
    password: ${DB_PASSWORD:change_to_a_strong_passworD123}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.unisantos.clinica_api: DEBUG
    org.springframework.security: DEBUG

/**
 * AppointmentForm - appointment form fields (patient, psychologist, date, location)
 */
export function AppointmentForm() {
  // TODO: appointment form fields (patient, psychologist, date, location)
  // TODO: Gate restricted fields and actions with usePermissions().can()
  // NO FUNCTIONAL CODE - Implementation guide only
  return null;
}

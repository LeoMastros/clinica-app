/**
 * SessionCancelDialog - cancel dialog (psychologist only, reason required for in-person)
 */
export function SessionCancelDialog() {
  // TODO: cancel dialog (psychologist only, reason required for in-person)
  // TODO: Gate restricted fields and actions with usePermissions().can()
  // NO FUNCTIONAL CODE - Implementation guide only
  return null;
}

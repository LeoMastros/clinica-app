services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: clinica-frontend
    ports:
      - "127.0.0.1:3000:80"
    restart: always

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: clinica-backend
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD}
<<<<<<< HEAD
      # O Compose so injeta no container o que esta declarado aqui. Sem estas
      # linhas, por o valor no .env nao surte efeito nenhum.
      # Os padroes espelham os da aplicacao: quem nao definir nada continua com
      # o comportamento de desenvolvimento, em vez de receber valor vazio.
      CLINICA_JWT_SEGREDO: ${CLINICA_JWT_SEGREDO:-troque-este-segredo-apenas-para-desenvolvimento-local}
      CLINICA_JWT_VALIDADE: ${CLINICA_JWT_VALIDADE:-PT8H}
      CLINICA_CORS_ORIGENS: ${CLINICA_CORS_ORIGENS:-http://localhost:3000,http://localhost:5173}
      # Vazio de proposito quando nao definido: e assim que o AdminInicial sabe
      # que nao deve criar conta nenhuma.
      CLINICA_ADMIN_INICIAL_NOME: ${CLINICA_ADMIN_INICIAL_NOME:-Administrador}
      CLINICA_ADMIN_INICIAL_EMAIL: ${CLINICA_ADMIN_INICIAL_EMAIL:-}
      CLINICA_ADMIN_INICIAL_SENHA: ${CLINICA_ADMIN_INICIAL_SENHA:-}
=======
      COORDINATOR_EMAIL: ${COORDINATOR_EMAIL}
      COORDINATOR_PASSWORD: ${COORDINATOR_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      # Prod: set to the frontend origin (e.g. https://app.your-domain.com)
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      # Prod (HTTPS): must be true — marks the refresh cookie Secure.
      AUTH_COOKIE_SECURE: ${AUTH_COOKIE_SECURE:-false}
>>>>>>> upstream/main
    depends_on:
      mysql:
        condition: service_healthy
    restart: always

  mysql:
    image: mysql:8.4
    container_name: clinica-mysql
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: always

volumes:
  mysql_data:
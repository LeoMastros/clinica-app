package com.unisantos.clinica_api.appointment;

import org.springframework.stereotype.Repository;

// Plain shell — Appointment is not a JPA entity (no Appointment table in schema.dbml;
// scheduling is modeled by Therapy_Session).
@Repository
public class AppointmentRepository {}
